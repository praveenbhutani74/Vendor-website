{
  "framework": "vite",
  "installCommand": "pnpm install",
  "buildCommand": "cd artifacts/vendor-infra && pnpm build",
  "outputDirectory": "artifacts/vendor-infra/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}